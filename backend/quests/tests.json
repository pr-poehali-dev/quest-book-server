{
  "tests": [
    {
      "name": "GET / — список веток с квестами",
      "method": "GET",
      "path": "/?route=/",
      "expectedStatus": 200
    },
    {
      "name": "GET /branches без admin-key — 401",
      "method": "POST",
      "path": "/?route=/branches",
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}
